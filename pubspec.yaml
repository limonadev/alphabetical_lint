name: alphabetical_lint
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  analyzer:
  collection:
  custom_lint_builder:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
